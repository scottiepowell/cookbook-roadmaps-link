# Copy to ignored .env.public-ai and generate a distinct random value.
# The same value is supplied to the core proxy and private sidecar container.
AI_SIDECAR_OPERATOR_TOKEN=
# Set this to the same generated value. Core reads the first name; sidecar
# reads the second through its existing operator-gate contract.
AI_OPERATOR_GATE_TOKEN=
